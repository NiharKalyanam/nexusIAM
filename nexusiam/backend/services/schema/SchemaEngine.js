/**
 * NexusIAM Schema Engine
 *
 * Handles:
 *  1. Schema discovery (auto-pull from live apps like Okta, SCIM, DB columns)
 *  2. Custom attribute definitions (add your own fields on top of native schema)
 *  3. Attribute mapping rules (NexusIAM canonical <-> app native)
 *  4. Transformers & delimiters (split, concat, regex, lookup, script)
 */
